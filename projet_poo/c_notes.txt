classes
Console.WriteLine()
Console.Write()
$"{var}"
Console.ReadLine();
propriétés -> variables
méthodes -> fonctions
dotnet new console -n NOM
dotnet run
.sln les projets dans la solution
.csproj configuration du projet dépendancs, ect
.bin fichiers finaux
.obj fichiers temporaries pdt le processus
--------------------------------
Variables
char 'o'
(decimal) pour faire une division
int,char, bool, string, const, long, float, double, char, byte, decamal
int? si jamais il peut y avoir un null dans la var ou Nullable<int>
var = "oui" | assignation auto
stockés dans le heap
alors que classes stockés dans le stack
${ {} }
value = oui ?? non; -> si oui est null mets non
(int) 3 ||| 3 as int? ne déclenche pas d'erreur
int.MinValue
int.MaxValue
34.ToString("00") pour nbr digits
---------------------------------
Focntions
class non{
public static void oui(){
  }
 }
non.oui()
---------------------------------
arronie Math.Round(var , 2)
if(n is string)
typeof(var)
objet.GetType()
type.Parse(var)
bool = type.TryParse(var,out default) -> default valeir mis dans var
Convert.toint32
string.Contains("dog")
array.StartsWith("b")
object.ToString()
str.Trim() strip() python
string.IndexOf(".")
--------------------------------
Conditions
|| &&
if (oui>5){
}
else if{}
--------------------------------
Arrays
[1..^1] du début à la fin
[0..5]
string[] = new string[3];
string2[] = ["ooui","oui2"]
int[,] tab = new int[10, 11];
string.Length
string[] names = ["oui","non"]
foreach(string name in names)
String.Join(sep,aray);
--------------------------------
switch (var){
	case "potato"
	case "apple":
		xxxx;
		break;
	default:
		xxx;
		break;
}
------------------------------
for (int i = 0; i<1;i++){
}
------------------------------
do{
}
while(true);
-----------------------------
foreach(var item in collection)
------------------------------
méthodes
static appartiennent à la classe 
int additionner(int a, int b) => a+b;
int addd(params int[] nbrs)
Surcharge additionner(int a, int b)
addiqtonner(float a, float b)
----------------
var maListe= new ArrayList();
--
var MaHashtable = new Hashtablee();
maHashtavle["ouo"]=43;
forearch (dicitionnaryentry entry in myhastable)
--
pareil qu'au dessus mais avec 
var maliste=new List<int>
nombres.Add(1);
Dictionary = new Dicitineary<string,int>
string.Join(", ", nombres)
-----------------------
Structure 
données indépendatnes différence avec class

struct Point
{
	public Point(int t){
		x=t;
	}
	public int X;
	public int Y;
} 
Point point;
point.x =oui
Point oui = new Point(10);
public override string ToString(){
	
}
------------------------
énumérations
enum Carburant{Essence,Diesil, Gaz,Elec}
Carburant carb = Carburant.Essence
-----------------------
Class
Personne personne1=new();
class Personne{
	
}
class Personne(string nom, string prenom){
	public string Nom = nom;
	public string Prenim = prenom;
}
mot clé partial => pour mettre sur +sieurs fichiers genre des bouts de code un pue partout
parital class Person{
	int ouoi=1;
	}
	partial clas sPerson
{
	int non =2
}
--
static class exemple {
public static int Compteur=0
}
pas instancié=> uniquement var statiques dedans

